import { useState } from "react";

const Menu = ({items}) => {
    // const [itemList, setItemList] = useState([]);
    
    // const useEffect (()=>{
    //     setItemList(items);
    // },[itemList])

    // const itemList = items.map((i)=>i.itemName)
    console.log(items[0]);
    return (
        <div className="Menu">
        </div>
    );
}
export default Menu;